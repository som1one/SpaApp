# Варианты структуры бэкенда для SPA приложения

## Вариант 1: Python FastAPI (рекомендуется для SPA салона)

```
backend/
├── app/
│   ├── __init__.py
│   ├── main.py                 # Точка входа приложения
│   │
│   ├── core/                   # Ядро приложения
│   │   ├── __init__.py
│   │   ├── config.py          # Конфигурация (настройки, env переменные)
│   │   ├── security.py        # JWT, хеширование паролей
│   │   ├── database.py         # Подключение к БД
│   │   └── dependencies.py    # Общие зависимости (get_db, get_current_user)
│   │
│   ├── api/                    # API роуты
│   │   ├── __init__.py
│   │   ├── deps.py            # Зависимости для API
│   │   └── v1/                # Версия API
│   │       ├── __init__.py
│   │       ├── router.py      # Главный роутер (объединяет все endpoints)
│   │       └── endpoints/
│   │           ├── __init__.py
│   │           ├── auth.py     # /auth/login, /auth/register, /auth/verify
│   │           ├── users.py    # /users/*
│   │           ├── services.py # /services/*
│   │           ├── bookings.py # /bookings/*
│   │           └── profile.py  # /profile/*
│   │
│   ├── models/                 # SQLAlchemy модели (БД схема)
│   │   ├── __init__.py
│   │   ├── user.py
│   │   ├── service.py
│   │   ├── booking.py
│   │   └── base.py            # Базовый класс для моделей
│   │
│   ├── schemas/                # Pydantic схемы (валидация запросов/ответов)
│   │   ├── __init__.py
│   │   ├── user.py            # UserCreate, UserResponse, UserUpdate
│   │   ├── service.py
│   │   ├── booking.py
│   │   └── auth.py             # Token, Login, Register
│   │
│   ├── services/              # Бизнес-логика
│   │   ├── __init__.py
│   │   ├── auth_service.py
│   │   ├── user_service.py
│   │   ├── service_service.py  # Сервисы салона (массаж, спа и т.д.)
│   │   ├── booking_service.py
│   │   └── email_service.py   # Отправка email для верификации
│   │
│   ├── repositories/          # Работа с БД (CRUD операции)
│   │   ├── __init__.py
│   │   ├── user_repository.py
│   │   ├── service_repository.py
│   │   └── booking_repository.py
│   │
│   └── utils/                  # Утилиты
│       ├── __init__.py
│       ├── email.py            # Отправка email
│       ├── validators.py       # Дополнительные валидаторы
│       └── helpers.py
│
├── alembic/                    # Миграции БД
│   ├── versions/
│   └── env.py
│
├── tests/                      # Тесты
│   ├── __init__.py
│   ├── conftest.py            # Фикстуры pytest
│   ├── test_auth.py
│   ├── test_users.py
│   └── test_bookings.py
│
├── requirements.txt            # Зависимости
├── .env.example               # Пример переменных окружения
├── .gitignore
├── README.md
└── docker-compose.yml         # Для PostgreSQL, Redis и т.д.
```

**Плюсы:**
- Современный и быстрый
- Автоматическая документация (Swagger)
- Асинхронность из коробки
- Отличная типизация с Pydantic

**Технологии:**
- FastAPI
- SQLAlchemy (ORM)
- Alembic (миграции)
- Pydantic (валидация)
- PostgreSQL
- Redis (опционально)

---

## Вариант 2: Python Flask (классический подход)

```
backend/
├── app/
│   ├── __init__.py
│   ├── main.py                # Точка входа (Flask app factory)
│   │
│   ├── config.py              # Конфигурация
│   │
│   ├── extensions.py           # Расширения (db, jwt, cors и т.д.)
│   │
│   ├── models/                 # SQLAlchemy модели
│   │   ├── __init__.py
│   │   ├── user.py
│   │   ├── service.py
│   │   └── booking.py
│   │
│   ├── api/                    # Blueprints (роуты)
│   │   ├── __init__.py
│   │   ├── auth.py            # @auth_bp.route('/login')
│   │   ├── users.py
│   │   ├── services.py
│   │   └── bookings.py
│   │
│   ├── services/               # Бизнес-логика
│   │   ├── __init__.py
│   │   ├── auth_service.py
│   │   ├── user_service.py
│   │   └── booking_service.py
│   │
│   ├── utils/                  # Утилиты
│   │   ├── validators.py
│   │   ├── email.py
│   │   └── security.py
│   │
│   └── middleware/             # Промежуточное ПО
│       ├── __init__.py
│       └── auth_middleware.py
│
├── migrations/                 # Flask-Migrate
│   └── versions/
│
├── tests/
│   ├── __init__.py
│   └── test_*.py
│
├── requirements.txt
├── .env.example
└── README.md
```

**Плюсы:**
- Простота и гибкость
- Много документации и примеров
- Легко начать

**Технологии:**
- Flask
- Flask-SQLAlchemy
- Flask-JWT-Extended
- Flask-Migrate
- PostgreSQL

---

## Вариант 3: Node.js Express (TypeScript)

```
backend/
├── src/
│   ├── index.ts               # Точка входа
│   │
│   ├── config/                # Конфигурация
│   │   ├── database.ts        # Подключение к БД
│   │   ├── env.ts             # Переменные окружения
│   │   └── constants.ts
│   │
│   ├── models/                # Mongoose модели (MongoDB) или TypeORM
│   │   ├── User.ts
│   │   ├── Service.ts
│   │   └── Booking.ts
│   │
│   ├── routes/                # Роуты Express
│   │   ├── index.ts           # Объединение всех роутов
│   │   ├── auth.routes.ts
│   │   ├── users.routes.ts
│   │   ├── services.routes.ts
│   │   └── bookings.routes.ts
│   │
│   ├── controllers/           # Контроллеры (обработчики запросов)
│   │   ├── auth.controller.ts
│   │   ├── user.controller.ts
│   │   ├── service.controller.ts
│   │   └── booking.controller.ts
│   │
│   ├── services/              # Бизнес-логика
│   │   ├── auth.service.ts
│   │   ├── user.service.ts
│   │   ├── booking.service.ts
│   │   └── email.service.ts
│   │
│   ├── middleware/            # Промежуточное ПО
│   │   ├── auth.middleware.ts
│   │   ├── error.middleware.ts
│   │   └── validation.middleware.ts
│   │
│   ├── utils/                 # Утилиты
│   │   ├── jwt.util.ts
│   │   ├── bcrypt.util.ts
│   │   └── email.util.ts
│   │
│   └── types/                 # TypeScript типы
│       ├── user.types.ts
│       ├── booking.types.ts
│       └── auth.types.ts
│
├── tests/
│   ├── unit/
│   └── integration/
│
├── package.json
├── tsconfig.json
├── .env.example
└── README.md
```

**Плюсы:**
- Один язык для фронта и бэка (если используете JS/TS)
- Быстрая разработка
- Большое комьюнити

**Технологии:**
- Express.js
- TypeScript
- MongoDB + Mongoose ИЛИ PostgreSQL + TypeORM
- JWT для авторизации

---

## Вариант 4: Микросервисная архитектура (расширенная)

```
backend/
├── services/
│   ├── auth-service/          # Сервис аутентификации
│   │   ├── app/
│   │   │   ├── main.py
│   │   │   ├── models/
│   │   │   ├── routes/
│   │   │   └── services/
│   │   ├── requirements.txt
│   │   └── Dockerfile
│   │
│   ├── user-service/          # Сервис пользователей
│   │   └── ...
│   │
│   ├── booking-service/        # Сервис бронирований
│   │   └── ...
│   │
│   └── notification-service/  # Сервис уведомлений
│       └── ...
│
├── api-gateway/               # API Gateway (объединяет все сервисы)
│   └── ...
│
├── shared/                    # Общие библиотеки
│   ├── models/
│   └── utils/
│
└── docker-compose.yml         # Оркестрация всех сервисов
```

**Плюсы:**
- Масштабируемость
- Независимое развертывание
- Изоляция сервисов

**Минусы:**
- Сложность для небольших проектов

---

## Вариант 5: Django REST Framework (Python)

```
backend/
├── manage.py
├── config/                     # Настройки проекта
│   ├── __init__.py
│   ├── settings.py
│   ├── urls.py                # Главный urls.py
│   └── wsgi.py
│
├── apps/                      # Django приложения
│   ├── auth_app/
│   │   ├── models.py
│   │   ├── views.py           # DRF ViewSets
│   │   ├── serializers.py     # Сериализаторы
│   │   ├── urls.py
│   │   └── permissions.py
│   │
│   ├── users/
│   │   ├── models.py
│   │   ├── views.py
│   │   └── serializers.py
│   │
│   ├── services/               # Услуги салона
│   │   └── ...
│   │
│   └── bookings/
│       └── ...
│
├── core/                      # Общие компоненты
│   ├── permissions.py
│   ├── pagination.py
│   └── filters.py
│
├── migrations/                # Автоматические миграции Django
│
├── requirements.txt
├── .env.example
└── README.md
```

**Плюсы:**
- Все из коробки (админка, миграции, ORM)
- Хорошая документация
- Много готовых решений

**Технологии:**
- Django
- Django REST Framework
- PostgreSQL

---

## 🎯 Рекомендация для SPA салона

**Вариант 1 (FastAPI)** - оптимальный выбор, потому что:

1. **Быстрая разработка** - автоматическая документация API
2. **Современный стек** - асинхронность, типизация
3. **Хорошо для REST API** - отлично подходит для мобильного приложения
4. **Легко масштабировать** - при росте проекта легко добавить функции

### Основные эндпоинты для начала:

```
POST   /api/v1/auth/register      # Регистрация
POST   /api/v1/auth/login           # Вход
POST   /api/v1/auth/verify-email    # Подтверждение email
GET    /api/v1/services             # Список услуг
POST   /api/v1/bookings             # Создать бронирование
GET    /api/v1/bookings             # Список бронирований
GET    /api/v1/profile              # Профиль пользователя
```

---

## 📋 Что нужно создать для старта (FastAPI вариант):

1. **База данных**: PostgreSQL
2. **Миграции**: Alembic
3. **Аутентификация**: JWT токены
4. **Email сервис**: для отправки кодов подтверждения
5. **Валидация**: Pydantic схемы
6. **CORS**: для работы с Flutter приложением

---

**Выберите вариант, который лучше подходит под ваши знания и требования проекта!**

