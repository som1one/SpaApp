# Политика конфиденциальности Priroda Spa

Этот репозиторий содержит страницу политики конфиденциальности для мобильного приложения Priroda Spa.

## 🚀 Публикация на GitHub Pages

### Автоматическая публикация

1. Перейдите в настройки репозитория GitHub
2. Откройте раздел **Pages** в меню слева
3. В разделе **Source** выберите:
   - Branch: `main` (или `master`)
   - Folder: `/docs`
4. Нажмите **Save**

GitHub автоматически опубликует страницу по адресу:
`https://[ваш-username].github.io/[название-репозитория]/`

### Ручная публикация

Если вы хотите использовать другую ветку или папку:

1. Создайте ветку `gh-pages`:
   ```bash
   git checkout -b gh-pages
   ```

2. Скопируйте файл `index.html` в корень ветки

3. Закоммитьте и запушьте:
   ```bash
   git add index.html
   git commit -m "Add privacy policy page"
   git push origin gh-pages
   ```

4. В настройках Pages выберите ветку `gh-pages` и папку `/ (root)`

## 📝 Настройка контента

Перед публикацией обязательно замените следующие поля в `index.html`:

- `[Ваше юридическое название компании]` - название вашей компании
- `[ваш email для связи]` - контактный email
- `[ваш телефон]` - контактный телефон
- `[ваш физический адрес]` - физический адрес компании
- `[ваш сайт, если есть]` - адрес веб-сайта (или удалите эту строку)

## 🎨 Особенности дизайна

- ✅ Современный и чистый дизайн
- ✅ Адаптивная верстка (mobile-first)
- ✅ Плавные анимации и переходы
- ✅ Темная тема (автоматически при предпочтениях системы)
- ✅ Кнопка "Наверх" для удобной навигации
- ✅ Оглавление с якорными ссылками
- ✅ Оптимизировано для SEO

## 📱 Использование в приложении

Для использования в Flutter приложении:

1. Добавьте URL страницы в `pubspec.yaml` или константы
2. Используйте `WebView` или `url_launcher` для открытия страницы
3. Пример:
   ```dart
   final url = 'https://[ваш-username].github.io/[название-репозитория]/';
   await launchUrl(Uri.parse(url));
   ```

## 🔄 Обновление политики

При обновлении политики:

1. Обновите дату в `date-badge` в начале страницы
2. Внесите необходимые изменения в контент
3. Закоммитьте изменения:
   ```bash
   git add docs/index.html
   git commit -m "Update privacy policy - [дата]"
   git push
   ```

GitHub Pages автоматически обновит страницу в течение нескольких минут.

## 📄 Лицензия

Этот документ является частью проекта Priroda Spa.

